# behat.yml
default:
  suites:
    default:
      contexts:
        - RestContext:
            - http://localhost:8080
fpm:
  suites:
    default:
      contexts:
        - RestContext:
            - http://localhost/restler4/