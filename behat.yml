# behat.yml
default:
  gherkin:
    filters:
      tags: "~@cookie&&~@session"
  suites:
    default:
      contexts:
        - RestContext:
            - http://localhost:8080
fpm:
  suites:
    default:
      filters:
        tags: ~@explorer
      contexts:
        - RestContext:
            - http://localhost
